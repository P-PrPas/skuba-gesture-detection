# Core — backbone + feature pipeline (this is all the production pipeline needs)
mediapipe==1.0.1          # Pose + Hands via the Tasks API; CPU, 0 VRAM. Backbone LOCKED at Phase 1.
opencv-contrib-python==4.11.0.86   # mediapipe wants -contrib; keep them the same version
numpy>=1.26,<3            # mediapipe 1.0.1 dropped its numpy<2 pin; 2.x works (tested)

# Phase 1 backbone comparison ONLY — not imported by backbone/ or pipeline/
ultralytics==8.3.55       # YOLO-pose
rtmlib==0.0.16            # RTMPose (ONNX)
onnxruntime==1.29.0       # for rtmlib; swap to onnxruntime-gpu via requirements-phase1-gpu.txt for the CUDA benchmark
